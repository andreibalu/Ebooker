//
//  CoffeeTipProductID.swift
//  Pageless
//

import Foundation

/// The optional one-time support purchase. Keep this in sync with App Store Connect and
/// `Products.storekit`; the product is intentionally consumable so users can support the app
/// more than once and no entitlement is created.
enum CoffeeTipProductID {
    static let coffee = "andreibaludev.Pageless.tip.coffee"
}
