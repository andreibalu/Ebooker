# Claude Code skill infra — local only, never commit
.claude/
.agents/
.codex/
skills-lock.json
CLAUDE.original.md

# Xcode / macOS noise
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/

# App Store screenshot generator (Next.js project — not part of the iOS app)
screenshots/
